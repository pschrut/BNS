var ResultOptions = Class.create(Menu, {
    initialize : function($super, id, options) {
	    $super(id, options);
    },
    show : function($super, element) {
	    $super(element)
    }
});

var TaxonomySugestions = Class.create(Menu, {
    initialize : function($super, id, options) {
	    $super(id, options);
    },
    show : function($super, element) {
	    $super(element)
    }
});